/** GSAP Plugins */
gsap.registerPlugin(ScrollTrigger, Observer, ScrollToPlugin, Draggable, MotionPathPlugin);


/** GSAP Animation - S1 -  */
gsap.to("#algerie",{
    x: 100,
    rotation: 90,
    duration: 1,
})

gsap.to